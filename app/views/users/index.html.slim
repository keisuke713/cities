= provide(:title, 'All users')
.container
  h1
    | All users
  .index
    - @users.each do |user|
      .user
        h2.profile
          = user.name
        p.profile
          = shorted_text user
          = link_to '詳細を見る', user
      p
        = delete_user_link user
  = will_paginate
